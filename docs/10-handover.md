# Handover — สถานะระบบ ณ 2026-09-22

เอกสารนี้ **สั้นโดยตั้งใจ** ไว้ให้คนหรือ agent ที่เพิ่งเข้ามาเห็นภาพใน 5 นาทีว่า
"ตอนนี้อยู่ตรงไหน · ห้ามพลาดอะไร · ทำอะไรต่อ" — รายละเอียดเต็มอยู่ที่
[CLAUDE.md](../CLAUDE.md) (1,400 บรรทัด) และ `docs/01`–`docs/09`

> อัปเดตเอกสารนี้ทุกครั้งที่ deploy หรือเปลี่ยนการตัดสินใจระดับนโยบาย
> ถ้ามันล้าสมัยเมื่อไหร่ มันจะอันตรายกว่าไม่มีเลย

---

## 1. ตอนนี้อยู่ตรงไหน

| | |
|---|---|
| Production commit | `50032f4` (deploy 2026-09-22) |
| Web | https://gpservice.garage-pro.net — healthy |
| API | https://gpservice-api.garage-pro.net — healthy |
| Mobile | **ยังไม่ได้แจกให้ใครใช้** — รันได้เฉพาะเครื่อง dev |
| Migration | ไม่มี `(Pending)` ค้าง |

**deploy ล่าสุดเปลี่ยนอะไรที่ผู้ใช้รู้สึกได้:**
1. 🔴 **ต้องออกใบเสร็จก่อนถึงจะให้ลูกค้าเซ็นรับรถได้** (`HANDOVER_RECEIPT_REQUIRED`) —
   เปลี่ยนลำดับงานหน้าเคาน์เตอร์จริง ถ้าหน้าร้านเคยเซ็นก่อนเก็บเงินจะถูกบล็อกทันที
2. ทุกบทบาทปฏิบัติการ (รวมช่าง/หัวหน้าช่าง/หน้าร้าน) ส่งมอบรถ + ปิดงานได้
   — guard เรื่องเงินยังคำนวณจากข้อมูลจริงครบ ไม่ได้ผ่อน
3. แก้บั๊ก **จ๊อบค้างถาวร** (สถานะใบเสนอราคากับสถานะจ๊อบเดินคนละจังหวะ)
4. เมนูใหม่ "เวลาทำงานของช่าง" ใต้รายงาน (Lead/Manager)

**จับเวลาช่าง — เก็บข้อมูลเสร็จแล้ว แต่ยังใช้จริงไม่ได้**
backend + API + UI บนมือถือเสร็จและทดสอบกับข้อมูลจริงแล้ว แต่ **ตัวจับเวลาเป็นของแอปมือถือล้วน**
และแอปยังไม่ได้แจก ⇒ ตอนนี้ production มีแค่ API ที่พร้อมรับกับหน้าเว็บไว้ดู/แก้คาบ
👉 **ยังไม่มีข้อมูลเวลาจริงไหลเข้าระบบจนกว่าจะแจกแอป** — ดู [docs/09](09-technician-time-tracking.md)

---

## 2. เรื่องที่ยังไม่ตัดสินใจ (ค้างอยู่จริง ไม่ใช่ของเก่า)

### 2.1 dev กับ production ใช้ฐานข้อมูลตัวเดียวกัน ⚠️
ทั้งสองฝั่งชี้ไป `10.10.4.11 / GarageService` เหมือนกันเป๊ะ แปลว่า:
- ใครรัน `dotnet ef database update` จากเครื่อง dev **มีผลกับ API ที่ลูกค้าใช้อยู่ทันที**
- ข้อมูลที่สร้างตอนทดสอบเป็นแถวจริงใน production (สาขา 227 "Service Center Demo")
- ที่ผ่านมารอดมาได้เพราะ migration ทุกตัวเป็น **additive ล้วน** (ตารางใหม่ / คอลัมน์ nullable)
  **ถ้าวันไหนมี migration ที่ drop หรือ rename ของจริง ระบบจะพังทันทีโดยไม่ต้อง deploy อะไรเลย**

→ ต้องเลือกสักทาง: แยกฐาน dev ออกมา **หรือ** ตั้งกติกาว่าใครรัน migration ได้ตอนไหน

### 2.2 ต้องบอกหน้าร้านเรื่องลำดับ "จ่ายเงิน → ออกใบเสร็จ → เซ็นรับรถ"
กฎข้อ 1 ในหัวข้อ 1 มีผลบน production แล้ว **แต่ยังไม่ได้แจ้งผู้ใช้**

### 2.3 RBAC ของ Job/Intake endpoint ยังเปิดกว้าง
ตอนนี้ gate ด้วย `[RequireShiftSession]` อย่างเดียว — ใครก็ตามที่ login แล้วมีกะเปิดอยู่
สร้าง/เปลี่ยนสถานะจ๊อบได้หมด **ต้องปิดก่อนขยายวงผู้ใช้** (โมดูลจับเวลา/จัดซื้อ/รายงาน
ไม่ได้สืบทอดช่องนี้ เพราะ gate role ที่ service ทุกเมธอด)

---

## 3. ทำอะไรต่อ

**งานถัดไปที่ชัดที่สุด: รายงานประเมินประสิทธิภาพช่าง**
ออกแบบครบแล้วที่ [docs/09 §5/§8](09-technician-time-tracking.md) — เมตริก 8 ตัว · เว็บ 3 หน้า ·
หน้า "ผลงานของฉัน" บนมือถือ · **ตั้งใจเลื่อนไปทำด้วยข้อมูลจริง** หลังเก็บเวลาไปสัก 2–4 สัปดาห์
เพราะตั้งเป้าตัวเลขก่อนมีข้อมูลคือตั้งมั่ว และยังไม่รู้ว่า "ช่างรุมคันเดียว" เกิดบ่อยแค่ไหน
ซึ่งเปลี่ยนรูปรายงานทั้งหน้า

**แต่ต้องแจกแอปก่อน** ไม่งั้นรอไปก็ไม่มีข้อมูล

รายการงานค้างทั้งหมดอยู่ที่ CLAUDE.md หัวข้อ "ยังไม่ได้ทำ"

---

## 4. กับดักที่ทำให้เสียเวลาไปแล้วจริง

| | |
|---|---|
| `dotnet run` **ไม่ hot-reload** service/state machine | แก้โค้ดแล้วไม่ restart = เข้าใจผิดว่าโค้ดไม่ทำงาน เคยหลงมาหลายรอบ |
| ต้องต่อ **Garage Pro VPN** ก่อนเสมอ | `scutil --nc start "Garage Pro VPN"` — ไม่ต่อ = เข้าฐานไม่ได้เลย |
| `<img src>` กับ endpoint ที่ต้อง Bearer | ใช้ไม่ได้ ต้อง fetch เป็น blob — ทั้งเว็บและ Flutter เจ็บมาแล้วทั้งคู่ |
| Dart: ใช้ `List` เป็น family key ของ Riverpod | เทียบด้วย identity ⇒ ยิง API ไม่หยุด ใช้สตริงที่เรียงแล้วแทน |
| Guid PK ต้อง `ValueGeneratedNever()` | ไม่งั้น EF ยิง UPDATE แทน INSERT |
| ตัวเลขเวลา/เงินต้องใช้ฟอนต์ tabular | ไม่งั้นแถบนาฬิกาสั่นทั้งแถบทุกวินาที |

รายการเต็ม (พร้อมอาการและวิธีแก้) อยู่ที่ CLAUDE.md หัวข้อ **"บทเรียนที่เจ็บมาแล้ว"**
— **อ่านก่อนเริ่มแก้โค้ด คุ้มค่าเวลา 10 นาที**

---

## 5. คำสั่งที่ใช้บ่อยที่สุด

```bash
# ตรวจก่อน commit ทุกครั้ง
dotnet build AMD.AutoService.GaragePro.sln
dotnet test backend/AMD.AutoService.GaragePro.Tests     # ฐานปัจจุบัน 261 ผ่าน / 5 skipped
cd web && node_modules/.bin/tsc -b --force && node_modules/.bin/vite build
cd mobile && dart analyze lib/ test/ && flutter test    # ฐานปัจจุบัน 59 ผ่าน

# deploy production (web + API เท่านั้น ไม่รวม mobile)
./scripts/deploy-production.sh
```

สคริปต์ deploy จะ pull `main` บน server → build image → รัน migration → เปลี่ยน container →
ตรวจ health ทั้ง local และ public **และหยุดทันทีถ้าขั้นไหนไม่ผ่าน**

---

## 6. มารยาทในการบันทึก (สำคัญกับ agent มากกว่าคน)

- **อัปเดต CLAUDE.md หลังทำงานเสมอ** — สิ่งที่ทำจริง · กฎที่เปลี่ยน · **ผลตรวจที่รันจริง** · งานค้าง
- **อย่าเขียนว่า "เสร็จ" จากผลทดสอบเฉพาะส่วน** — แยกให้ชัดว่า "compile ผ่าน" กับ
  "ทดสอบกับข้อมูลจริงแล้ว" คนละเรื่องกัน เอกสารนี้ถูกใช้ตัดสินใจ deploy
- เจอโค้ดขัดนโยบาย → บันทึก `[RISK]` แล้วถามก่อน **ห้ามเขียนเอกสารย้อนหลังเพื่ออนุญาตตัวเอง**
- comment อธิบาย **ทำไม** ไม่ใช่ **อะไร** · ใส่ `[BIZ]` `[ASSUME]` `[RISK]` `[SECURITY]` ให้ตรงกับเอกสาร
